# Maintainer: WarmShower OS <admin@warmshower.ai>
# Upstream-Maintainer: Robin Candau <antiz@archlinux.org>
#
# TODO (WS-040): Fork Antiz96/arch-update → Warm-shower/warmshower-update
# and update source= URL once fork is created. For now, pointing to upstream
# arch-update at the version that was previously used.

pkgname=warmshower-update
pkgver=3.20.2
pkgrel=1
pkgdesc="An update notifier & applier that assists you with important pre / post update tasks"
url="https://github.com/Warm-shower/warmshower-update"
arch=('any')
license=('GPL-3.0-or-later')
depends=('bash' 'systemd' 'pacman' 'pacman-contrib' 'archlinux-contrib' 'curl' 'fakeroot' 'util-linux'
         'htmlq' 'diffutils' 'hicolor-icon-theme' 'python' 'python-pyqt6' 'qt6-svg' 'glib2' 'xdg-utils')
makedepends=('scdoc' 'git')
checkdepends=('bats')
conflicts=('arch-update')
optdepends=('paru: AUR Packages support'
            'yay: AUR Packages support'
            'pikaur: AUR Packages support'
            'flatpak: Flatpak Packages support'
            'libnotify: Desktop notifications support on new available updates'
            'alhp-utils: Check for ALHP build queue or outdated mirrors'
            'vim: Default diff program for pacdiff'
            'neovim: Default diff program for pacdiff if EDITOR=nvim'
            'sudo: Privilege elevation'
            'sudo-rs: Privilege elevation'
            'opendoas: Privilege elavation')
# Temporary: using upstream arch-update source until Warm-shower/warmshower-update fork is created (WS-040)
source=("git+https://github.com/Antiz96/arch-update#tag=v${pkgver}")
sha256sums=('SKIP')

prepare() {
    cd "${pkgname}"
    make clean
}

build() {
    cd "${pkgname}"
    make
}

check() {
    cd "${pkgname}"
    make test
}

package() {
    cd "${pkgname}"
    make PREFIX=/usr DESTDIR="${pkgdir}" install

    ln -s arch-update "${pkgdir}/usr/bin/${pkgname}"
}
