# Maintainer: WarmShower OS <admin@warmshower.ai>
# Upstream-Maintainer: Vladislav Nepogodin <upstream-maintainer>

pkgname=warmshower-wallpapers
pkgdesc='Wallpapers for WarmShower OS'
pkgver=r10.fb98756
pkgrel=1
arch=('any')
url="https://github.com/Rawknee-69/$pkgname"
license=(GPL-1.0-only)
makedepends=('git')
conflicts=('warmshower-wallpapers')
replaces=('warmshower-wallpapers')
source=("git+$url.git")
sha512sums=('SKIP')

pkgver() {
   cd $srcdir/$pkgname
   printf "r%s.%s" "$(git rev-list --count HEAD)" "$(git rev-parse --short HEAD)"
}

package() {
    install -d $pkgdir/usr
    cp -rf $srcdir/$pkgname/usr $pkgdir
}
